const a = window.prompt("Number 1");
const b = window.prompt("Number 2");
const c = Number(a) + Number(b);

window.alert(`${a}+${b} = ${c}`);

const user_age = window.prompt("나이를 입력하세요");
const year = 2025 - Number(user_age);

window.alert(`귀하의 태어난 년도는 ${year}년도 입니다!`);
